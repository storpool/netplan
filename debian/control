Source: netplan
Section: python
Priority: optional
Maintainer: Peter Pentchev <pp@storpool.com>
Build-Depends:
 debhelper-compat (= 12),
 dh-python,
 python,
 python-all,
 python-setuptools,
 python-yaml,
 python3-all,
 python3-setuptools,
 python3-yaml
Standards-Version: 4.3.0
Homepage: https://github.com/storpool/netplan/
Vcs-Git: https://github.com/storpool/netplan.git -b ubuntu/master
Vcs-Browser: https://github.com/storpool/netplan/tree/ubuntu/master
Rules-Requires-Root: no

Package: python-netplan
Architecture: all
Provides:
 ${python:Provides},
Depends:
 ${misc:Depends},
 ${python:Depends},
 python-yaml
Recommends:
 ${python:Recommends},
Suggests:
 ${python:Suggests},
Description: Python 2.x library for parsing the netplan configuration data
 This module parses the YAML configuration files describing the system's
 network configuration in the format used by the netplan.io package.
 The main parser is the `netplan.parser.Parser` class (also exported as
 `netplan.Parser`); its `parse()` method returns a data structure of
 the `netplan.config.NetPlan` class (also exported as `netplan.NetPlan`).
 .
 This package provides the Python 2.x library.

Package: python3-netplan
Architecture: all
Provides:
 ${python3:Provides},
Depends:
 ${misc:Depends},
 ${python3:Depends},
 python3-yaml
Recommends:
 ${python3:Recommends},
Suggests:
 ${python3:Suggests},
Description: Python 3.x library for parsing the netplan configuration data
 This module parses the YAML configuration files describing the system's
 network configuration in the format used by the netplan.io package.
 The main parser is the `netplan.parser.Parser` class (also exported as
 `netplan.Parser`); its `parse()` method returns a data structure of
 the `netplan.config.NetPlan` class (also exported as `netplan.NetPlan`).
 .
 This package provides the Python 3.x library.

Package: netplan-parser
Section: utils
Architecture: all
Depends:
 ${misc:Depends},
 ${python3:Depends},
 python3-netplan (= ${binary:Version}),
 python3-yaml
Description: command-line tool for parsing the netplan configuration data
 This module parses the YAML configuration files describing the system's
 network configuration in the format used by the netplan.io package.
 The main parser is the `netplan.parser.Parser` class (also exported as
 `netplan.Parser`); its `parse()` method returns a data structure of
 the `netplan.config.NetPlan` class (also exported as `netplan.NetPlan`).
 .
 This package provides the `netplan-parser` command-line utility.
